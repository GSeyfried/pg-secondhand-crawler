const median=values=>{const x=[...values].sort((a,b)=>a-b);if(!x.length)return null;const m=Math.floor(x.length/2);return x.length%2?x[m]:(x[m-1]+x[m])/2;};
export function marketAvailability(histories,equipmentType,filterDescription,asOf=new Date().toISOString()){const relevant=histories.filter(h=>h.equipmentType===equipmentType||h.observations.at(-1)?.equipmentType===equipmentType),firstDates=relevant.map(h=>Date.parse(h.firstSeenAt)).sort((a,b)=>a-b),gaps=firstDates.slice(1).map((x,i)=>(x-firstDates[i])/86400000),activeDays=relevant.map(h=>Math.max(0,(Date.parse(h.lastSeenAt??asOf)-Date.parse(h.firstSeenAt))/86400000)),window=firstDates.length?Math.max(1,(Date.parse(asOf)-firstDates[0])/86400000):0;return{equipmentType,filter:filterDescription,qualifyingListingsObserved:relevant.length,observationWindowDays:Math.round(window),estimatedListingsPerMonth:window?Number((relevant.length/window*30).toFixed(2)):0,medianDaysBetweenListings:median(gaps),medianDaysActive:median(activeDays),frequency:relevant.length<2?'unknown':window/relevant.length>30?'rare':'common'};}
