const $=selector=>document.querySelector(selector),money=(amount,currency='USD')=>amount==null?'Unknown':new Intl.NumberFormat('en-US',{style:'currency',currency,maximumFractionDigits:0}).format(amount),label=value=>(value??'').replaceAll('-',' ').replace(/\b\w/g,x=>x.toUpperCase());let activeJob=null,pollTimer=null;
function empty(text){return`<p class="empty">${text}</p>`}function render(data){const p=data.plan,r=data.report,currency=p.maximumTotalBudget.currency;$('#last-updated').textContent=`Updated ${new Date(r.generatedAt).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}`;$('#hero').classList.remove('skeleton');$('#hero').innerHTML=`<div><p class="eyebrow" style="color:#c7df78">${label(data.phase)} phase · ${label(r.deadlineRisk)} risk</p><h2>${p.requiredComponentsRemaining.length?`${p.requiredComponentsRemaining.length} core components remain`:'Core kit complete'}</h2><p>Targeting a compatible low EN-B setup by ${new Date(`${p.targetCompletionDate}T12:00:00`).toLocaleDateString(undefined,{month:'long',day:'numeric',year:'numeric'})}, with uncertainty and safety checks kept visible.</p></div><div class="countdown"><strong>${data.daysRemaining}</strong><span>DAYS REMAINING</span></div>`;const metrics=[['Maximum budget',money(p.maximumTotalBudget.amount,currency)],['Committed spend',money(p.committedSpend,currency)],['Available after reserve',money(p.remainingBudget,currency)],['Components purchased',`${p.purchasedComponents.length} / 4`]];$('#metrics').innerHTML=metrics.map(x=>`<div class="metric"><small>${x[0]}</small><strong>${x[1]}</strong></div>`).join('');$('#scenarios').innerHTML=data.scenarios.strategies.map(s=>`<article class="scenario"><div><h3>${label(s.name)}</h3><p>${s.reasons?.[0]??''}</p><div class="chips">${s.missingComponents.map(x=>`<span class="chip warn">Missing ${x}</span>`).join('')}${s.components.map(x=>`<span class="chip">${x.equipmentType}: ${x.title}</span>`).join('')}</div></div><div><div class="price">${money(s.minimumProjectedTotal,currency)}+</div><small>${s.qualifying?'Qualifying':'Needs verification'}</small></div></article>`).join('');$('#watchlist').innerHTML=data.watchMatches.length?data.watchMatches.slice(0,6).map(x=>`<div class="list-item"><div><a href="${x.sourceUrl}" target="_blank" rel="noopener">${x.title}</a><small>${x.manualReview.length?x.manualReview.join(' · '):'Requirements matched'}</small></div><strong>${money(x.price?.amount,x.price?.currency)}</strong></div>`).join(''):empty('No current listing matches the saved watch rules.');$('#drops').innerHTML=data.priceDrops.length?data.priceDrops.map(x=>`<div class="list-item"><div><strong>${x.percentDrop}% drop</strong><small>${x.daysSinceLastChange} days since change</small></div><span>${money(x.currentPrice,x.currency)}</span></div>`).join(''):empty('No observed price reductions yet.');$('#action').textContent=r.recommendedAction;$('#action-context').textContent=`${data.historyCount} listings tracked · ${data.watchMatches.length} watch matches · ${r.manualReviewCount} manual reviews`;const groups=data.marketStats.filter(x=>x.manufacturer==null).slice(0,6);$('#markets').innerHTML=groups.length?groups.map(x=>`<div class="market-row"><strong>${label(x.equipmentType)}</strong><div><div class="bar"><i style="width:${Math.min(100,(x.sampleSize/10)*100)}%"></i></div><small>n=${x.sampleSize} · ${x.strength}</small></div><span>${money(x.medianPrice,x.currency)}</span></div>`).join(''):empty('Run market statistics after collecting more observations.');$('#new-listings').innerHTML=data.newListings.length?data.newListings.map(x=>`<div class="list-item"><div><a href="${x.sourceUrl}" target="_blank" rel="noopener">${x.title}</a><small>${new Date(x.firstSeenAt).toLocaleDateString()}</small></div></div>`).join(''):empty('No historical observations yet.');$('#command-grid').innerHTML=data.localOnly?data.commands.map(x=>`<button class="command-card" data-command="${x.id}"><strong>${x.label}</strong><small>${commandHelp[x.id]??'Run local workflow'}</small><span class="run">Run →</span></button>`).join(''):`<article class="command-card"><strong>Hosted read-only mode</strong><small>Command execution is disabled on Vercel. Run jobs locally until an authenticated queue is connected.</small></article>`;document.querySelectorAll('[data-command]').forEach(button=>button.onclick=()=>runCommand(button.dataset.command,button.querySelector('strong').textContent));}
const commandHelp={'crawl-fixtures':'Validate parsers without network requests.','crawl-live-five':'Fetch five public listings; production stays untouched.','market-stats':'Recalculate asking-price ranges and sample strength.','watchlist':'Match current listings against saved rules.','plan':'Rebuild cheapest, value, and sooner strategies.','report':'Refresh budget, deadline risk, and next action.','tests':'Run the complete local regression suite.'};
async function load(){try{const response=await fetch('/api/dashboard'),data=await response.json();if(!response.ok)throw new Error(data.error);render(data);}catch(error){$('#hero').classList.remove('skeleton');$('#hero').innerHTML=`<div><h2>Dashboard data unavailable</h2><p>${error.message}</p></div>`;}}
async function runCommand(id,title){clearTimeout(pollTimer);$('#job-title').textContent=title;$('#job-status').textContent='Starting';$('#job-output').textContent='Starting local job…';const response=await fetch(`/api/commands/${id}`,{method:'POST'}),job=await response.json();if(!response.ok){$('#job-output').textContent=job.error;return;}activeJob=job.id;pollJob();}
async function pollJob(){if(!activeJob)return;const response=await fetch(`/api/jobs/${activeJob}`),job=await response.json();$('#job-status').textContent=label(job.status);$('#job-output').textContent=job.output||'Running…';if(job.status==='running')pollTimer=setTimeout(pollJob,800);else{activeJob=null;await load();}}
document.querySelectorAll('.nav').forEach(button=>button.onclick=()=>{document.querySelectorAll('.nav,.view').forEach(x=>x.classList.remove('active'));button.classList.add('active');$(`#${button.dataset.view}`).classList.add('active');});$('#refresh').onclick=load;load();
